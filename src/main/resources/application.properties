# Application Name
spring.application.name=home-item-inventory

# 1. Database Properties for External MariaDB
#spring.datasource.username=${DB_USER}
#spring.datasource.password=${DB_PASSWORD}
#spring.datasource.url=${DB_URL}
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
#spring.jpa.hibernate.ddl-auto=update
# OR
# 2. Database Properties for Use in memory H2 DB - use for testing!
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console
spring.datasource.url=jdbc:h2:mem:testdb
#spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
# Persist H2 database
#spring.datasource.url = jdbc:h2:file:C:/data/sample (Windows only)

# Application port
server.port=${SERVER_PORT}

# Spring Security - ignored if you set up any kind of
# UserDetailsService or use the Fluent API in the application WebSecurityConfig.java
spring.security.user.name=${LOGIN_USER}
spring.security.user.password=${LOGIN_PASSWORD}
